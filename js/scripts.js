$(document).ready(function(){
    
    var options = {
        animateThreshold: 150, //pixeles de aparicion
        scrollPollInterval: 20 //tiempo de aparicion
    }
    $('.aniview').AniView(options);
    
});